[project]
name = "mpm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12"
dependencies = [
    "pandas==2.2.3",
    "polars>=1.17.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
